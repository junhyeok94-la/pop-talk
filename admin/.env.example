# admin 서버가 api를 부를 주소.
#
# 브라우저는 이 값을 모른다. 브라우저는 admin의 /api를 부르고,
# admin 서버가 뒤에서 여기로 넘긴다 (next.config.ts의 rewrites).
# 그래서 NEXT_PUBLIC_ 접두어가 없다 — 이 값은 서버에만 머문다.
#
# 비워두면 Private ALB의 WAS 3200 포트를 사용한다.
# api를 was로 옮기면 그 주소를 넣는다.
API_PROXY_TARGET=http://popcorn-private-alb-144239326-fdd5172f3bd6.kr.lb.naverncp.com:3200
